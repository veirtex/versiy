# POSTGRES
POSTGRES_DB=versiy
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# REDIS
REDIS_PASSWORD=changeme

# APP
ADDR=localhost:3000
POSTGRES_ADDR=postgres://postgres:postgres@localhost:5432/versiy?sslmode=require
# SECRET MUST be at least 32 characters for security
SECRET=changemechangemechangemechangeme
DEFAULT_DOMAIN=https://api.versiy.cc/
APP_PORT=8080
ENVIRONMENT=dev

# Security Notes:
# - SSL Mode Options: disable, allow, prefer, require, verify-ca, verify-full
# - For development: use sslmode=disable or sslmode=prefer
# - For production: use sslmode=require or sslmode=verify-full
# - SECRET must be at least 32 characters (cryptographically secure random value)
# - REDIS and PostgreSQL connections now use TLS encryption by default
